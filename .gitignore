.venv/
.ruff_cache/
__pycache__/
*.pyc
*.egg-info/
*.log
.DS_Store
.env

# Data: raw synth output is regeneratable; processed train/val is committed.
data/raw/
data/interim/

# Trainer outputs (checkpoints, adapters, intermediate state).
runs/*/checkpoints/
runs/*/loss.jsonl
trainer_state*.json

# Merged + quantized models — pull from HF Hub on demand.
models/

# Eval reports — regenerated per run.
eval/reports/

# RunPod state.
.runpod_pod_id
